{{/* Template: verify-analysis — used by zforge verify on FAIL */}}
You are debugging failing Rust tests.

## Input Files
{{context_files}}

## Failed Tests
{{failed_tests}}

## Your Job
1. Inspect verify.md for only the relevant failure blocks
2. Analyze why each listed test failed
3. Identify root cause:
   - Implementation bug
   - Test setup error (wrong mock, missing fixture)
   - Test itself is wrong
4. Suggest a specific fix for each failure
5. Check whether testspec.md case IDs are covered

## Output
For each failed test:
### test_name
- Failure type: [implementation bug / test setup / wrong test]
- Root cause: [specific reason]
- Fix: [concrete code change]

## Summary
- Fix in implementation: [list]
- Fix in tests: [list, if any tests were wrong]
- Missing coverage: [list, if any AC not covered]
