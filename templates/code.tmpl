{{/* Template: code — used by zforge code */}}
You are implementing a {{language}} feature.

## Input Files
{{context_files}}

## Fallback References
Open these only if the plan or testspec is ambiguous:
{{spec_ref}}
{{task_ref}}

{{if failed_tests}}
## Previous Attempt Failed — Verifier Feedback

The last code attempt did not pass the verifier. Read the verify report carefully
before changing anything.

Failing tests:
{{failed_tests}}

Verify report:
{{verify_ref}}

Retry instructions:
- Re-read `plan.md` and `testspec.md` — confirm the expected behavior.
- Inspect each failing test — understand expected vs actual.
- Make the smallest possible change to make the failing tests pass.
- Do not modify the tests themselves unless `testspec.md` itself is wrong.
- Do not break any test that was passing before.

{{end}}
## Code Search

When you need to locate existing code, understand call sites, or find related symbols:
- `mcp__codegraph__query` — find functions, types, traits, modules by name or pattern
- `mcp__codegraph__context` — get semantic context for the area you're changing
- `mcp__codegraph__affected` — discover which tests are affected by your changes
- `mcp__codegraph__files` — browse file structure

Prefer these over grep/find for accuracy. Fall back to Grep / Glob if the server is not registered.

## Instructions
1. Read plan.md and testspec.md first.
2. Write failing tests for the testspec case IDs.
3. Implement the feature — organize code by feature, not by layer
4. Keep related things together: handler, service logic, and data access can live in the same module if they're small
5. Extract shared logic only when it's actually reused
6. Run tests — all must pass
7. Verify coverage meets testspec.md targets

## Constraints
- Language: {{language}}
- Test command: {{test_command}}
- Execute steps in the exact order defined in plan.md
- All tests in testspec.md must pass before marking done
- Keep changes minimal — do not touch code unrelated to the current task
- Preserve existing public interfaces unless the spec explicitly changes them
- Organize by feature/concern, not by abstract layers
- Keep modules small and focused — split when a file gets too large, not before
- Prefer simple, flat structures over deep hierarchies

## Completion Checklist

Before reporting done, verify:
- Every test case in testspec.md exists in the test suite and passes
- Each plan.md step landed (or deviation is flagged in the summary)
- No public interface changed outside spec.md authorization
- No panic-style placeholders in production paths
- On retry: every previously-failing test now passes and root cause noted

## Do Not Do

- Do not skip writing tests first — write the failing test, then the implementation
- Do not exceed the scope approved in spec.md without flagging it explicitly
- Do not silently change function signatures, return types, or error variants
- Do not delete or rename tests without a documented reason
- Do not open spec.md or task.md unless the approved plan/testspec leaves a gap
- Do not refactor unrelated code even if it looks messy
- Do not leave panic-style placeholders in production paths. Examples by language:
  - Rust: `unwrap()`, `expect("TODO")`, `todo!()`, `unimplemented!()`, `panic!()`
  - Python: `raise NotImplementedError`, bare `pass` where logic is required
  - JS/TS: `throw new Error("TODO")`, `// TODO` in lieu of real code
  - Go: `panic("TODO")`

## When Done
Run: zforge verify {{task_id}}
